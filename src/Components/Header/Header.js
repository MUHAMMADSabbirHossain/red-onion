import React from 'react';
import "./Header.css";
import Banner from '../Banner/Banner';

const Header = () => {
    return (
        <header>
            header
            <section>
                <Banner></Banner>
            </section>
        </header>
    );
};

export default Header;